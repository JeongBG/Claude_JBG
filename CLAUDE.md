# CLAUDE.md

이 파일은 이 저장소에서 작업할 때 Claude Code(claude.ai/code)에게 제공되는 가이드입니다.

## 프로젝트 개요

무작위로 명언을 보여주는 단일 정적 페이지 `index.html`로 구성되어 있습니다. 빌드 시스템, 패키지 매니저, 백엔드가 없으며 HTML, CSS, JS가 모두 하나의 파일 안에 인라인으로 작성되어 있습니다.

## 개발

빌드/린트/테스트 도구가 없습니다. 변경 사항을 확인하려면 브라우저에서 파일을 직접 엽니다:

```bash
open index.html
```

## 아키텍처

마크업, 스타일, 스크립트가 모두 `index.html` 하나에 들어 있습니다:

- 명언은 인라인 `<script>` 안에 `{ text, author }` 객체 배열로 하드코딩되어 있습니다.
- "다음 명언" 버튼은 무작위로 명언을 선택하며, `lastIndex`를 이용해 바로 직전과 같은 명언이 다시 나오지 않도록 합니다.
- "복사하기" 버튼은 `navigator.clipboard`를 통해 현재 명언을 클립보드에 복사하며, 해당 API를 사용할 수 없을 경우 `alert()`로 대체합니다.

기능을 추가할 때는, 프로젝트 규모가 커져 별도의 `.css`/`.js` 파일로 분리할 필요가 생기기 전까지는 단일 파일 구조를 유지하세요.
